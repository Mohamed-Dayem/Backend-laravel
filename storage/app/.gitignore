# This is a .gitignore file for a public directory
# It specifies which files and directories to ignore when committing to version control

# Ignore all files and directories (except those explicitly allowed)
*

# However, the following files and directories should be included
!.htaccess
!.htpasswd
!index.html
!public/  # Include the entire public directory

# Within the public directory, ignore all files and directories (except those explicitly allowed)
public/*

# But include the following specific files
!public/css/style.css
!public/js/script.js
!public/images/logo.png
