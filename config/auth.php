<?php

return [

    /*
    |--------------------------------------------------------------------------
    | Authentication Defaults
    |--------------------------------------------------------------------------
    | This section defines the default authentication settings for your
    | application. The 'guard' option sets the default authentication guard,
    | which is used to protect routes, and the 'passwords' option sets the
    | default passwords table for password reset functionality.
    |
    */

    'defaults' => [
        'guard' => 'web', //
