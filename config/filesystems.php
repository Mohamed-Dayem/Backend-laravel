<?php

return [

    // Specifies the default filesystem disk used by the framework.
    // Supported drivers: 'local', 'ftp', 'sftp', 's3'.
    'default' => env('FILESYSTEM_DISK', 'local'),

    /*
    |--------------------------------------------------------------------------
    | Filesystem Disks
    |------------------------------------------------
