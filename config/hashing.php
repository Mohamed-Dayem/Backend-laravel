<?php

// This is a configuration file for password hashing in an application.

return [

    /*
    |--------------------------------------------------------------------------
    | Default Hash Driver
    |--------------------------------------------------------------------------
    |
    | This option specifies the default hash driver used to hash passwords for
    | the application. The supported drivers are: "bcrypt", "argon", and
    | "argon2id".
    |
    */
    'driver' => 'bcrypt',

    /*
    |--------------------------------------------------------------------------
    | Bcrypt Options
    |------------------------------------------------
